# Python
backend/venv/
backend/__pycache__/
**/__pycache__/
*.pyc

# Backend runtime data (SQLite DB + recorded audio/spectrograms)
backend/data/
backend/server.log

# Node
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo

# Editor/OS
.DS_Store

phonemizer_deploy_key
phonemizer_deploy_key.pub
*.key

# Server-side secrets (Google OAuth client id/secret, etc.) — loaded by
# `docker compose` from the deploy directory, never committed.
.env
